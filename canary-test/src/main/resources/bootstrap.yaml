spring:
  cloud:
      config:
        uri: http://localhost:8781
        fail-fast: true
        username: user
        password: 123
        retry:
          initial-interval: 2000
          max-interval: 10000
          multiplier: 2
          max-attempts: 10
